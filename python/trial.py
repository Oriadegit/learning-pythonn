def login(password):
    if len(password)>5:
        print('password acceptable')
    else:
        print('invalid password ' + password)

login('adefemi')
login('mife')

list = ['ademi','']

#create a function to calculate the area of a circle, triangle, paralelogram, rectangle and the perimeter of a rectangle
#create a function for "almighty formula"